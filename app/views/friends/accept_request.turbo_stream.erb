<%= turbo_stream.prepend 'friends', partial: 'friend', locals: {friend_2: @user.id, friend_1: current_user.id} %>
<%= turbo_stream.remove "make_friends" %>
<%= turbo_stream.update "requests", partial: "requests", locals: {friend_2: @user.id, friend_1: current_user.id}%>