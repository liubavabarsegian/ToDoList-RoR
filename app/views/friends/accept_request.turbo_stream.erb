<%= turbo_stream.prepend 'friends', partial: 'friend', locals: {friend2: @user.id, friend1: current_user.id} %>
<%= turbo_stream.remove "make_friends" %>
<%= turbo_stream.update "requests", partial: "requests", locals: {friend2: @user.id, friend1: current_user.id}%>